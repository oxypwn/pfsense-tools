--- pf.c.old	2008-11-11 22:51:23.000000000 +0100
+++ pf.c	2008-11-11 22:53:04.000000000 +0100
@@ -6884,6 +6884,10 @@
 
 #ifdef __FreeBSD__
 	M_ASSERTPKTHDR(m);
+
+	/* Check to avoid breaking ipfw forward rules needed on CP */
+	if (m->m_flags & M_FASTFWD_OURS) 
+		return (PF_PASS);
 #else
 #ifdef DIAGNOSTIC
 	if ((m->m_flags & M_PKTHDR) == 0)
