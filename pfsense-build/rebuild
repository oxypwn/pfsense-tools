LOCALDIR=$PWD

rm -rf /usr/local/freesbie-clone
for i in .done_clonefs .done_extra .done_iso .done_pkginstall;
do
	rm /home/pfsense/freesbie2/$i
done

mkdir -p /home/pfsense/tools/pfsense-build/customroot

sh -x pfsense.sh
qemu -cdrom /usr/obj/FreeSBIE.iso
